[package]
name = "sandbox-egress-host-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
sandbox-egress = { path = "../.." }

[lints.rust]
unsafe_code = "forbid"
